type Query {
  user(id: ID!): User!
  users(name: String): [User!]
  usersCursor(after: String, first: Int): UsersResultCursor
  company(id: ID!): Company!
  companies: [Company!]
}

type Mutation {
  updateUser(user: UserInput!): Boolean
}

schema {
  query: Query
  mutation: Mutation
}
